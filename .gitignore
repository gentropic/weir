# OS
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
.#*

# Node (preemptive — for future linters, web-ext, etc.)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.pnpm-store/

# AI assistants
.claude/
.claude.local.md
CLAUDE.md
.cursor/
.cursorrules
.aider*
.specstory/

# Chrome extension packaging
*.crx
*.pem
web-ext-artifacts/

# Build / dist
dist/
build/
*.log

# Personal data — curated feed sets, exports, notes. Never committed.
personal/

# In-flight spec drafts — staged here while being argued/implemented, then their
# durable decisions graduate into the tracked docs (SPEC.md/GLASS.md) and the draft
# is deleted. Working context, not a published artifact (like CLAUDE.md).
spec_inbox/

# generated label list (run off on the Brother; regenerate from CLASS_NAMES)
tools/shelf-labels.txt
tools/label-placement.html
tools/*.stl
